age = input("Write your age: ")
new_age = int(age) + 15
print ("New age by adding 15 is: " + str(new_age)) #conccat two string here.

# NOTE: can not add one string to the number so we use str() and int() we can also use float().
